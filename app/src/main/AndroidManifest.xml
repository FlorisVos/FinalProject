<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.floris.onceuponatime">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Reader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".story_activity" />
        <activity android:name=".AnimatedTextActivity" />
        <activity android:name=".TypeWriterView_activity" />
        <activity android:name=".Fileselect" />
        <activity android:name=".WikiActivity" />
        <activity android:name=".Welcome_activity" />
        <activity android:name=".fragment_webview"></activity>
    </application>

</manifest>